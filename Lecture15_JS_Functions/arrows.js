function add(x, y) {
    return x + y; 
}

const add2 = function(x, y) {
    return x + y; 
}

const addArrow = (x, y) => {
    return x + y; 
}

const addArrow2 = (x, y) => x + y; 

let sum = addArrow2(100, 200); 
console.log(sum); 




